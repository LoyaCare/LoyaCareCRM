{
  "scripts": {
    "generate": "prisma generate && cp -R ./prisma/client.ts ../backend/src/prisma && cp -R ./generated/prisma-client/* ../frontend/src/shared/generated/prisma-client/ && cp -R ./generated/prisma-client/* ../backend/generated/prisma-client/",
    "generate_": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "@types/uuid": "^10.0.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "prisma": "^6.13.0"
  }
}
